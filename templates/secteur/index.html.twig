{% extends 'base.html.twig' %}

{% block title %}Secteur index{% endblock %}

{% block body %}
    <h1>Secteur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Nom</th>
                <th>Creepar</th>
                <th>Datecreation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for secteur in secteurs %}
            <tr>
                <td>{{ secteur.id }}</td>
                <td>{{ secteur.code }}</td>
                <td>{{ secteur.nom }}</td>
                <td>{{ secteur.creepar }}</td>
                <td>{{ secteur.datecreation ? secteur.datecreation|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_secteur_show', {'id': secteur.id}) }}">show</a>
                    <a href="{{ path('app_secteur_edit', {'id': secteur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_secteur_new') }}">Create new</a>
{% endblock %}
