{% extends 'base.html.twig' %}

{% block title %}ErpModeleVehicule index{% endblock %}

{% block body %}
    <h1>ErpModeleVehicule index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for erp_modele_vehicule in erp_modele_vehicules %}
            <tr>
                <td>{{ erp_modele_vehicule.id }}</td>
                <td>{{ erp_modele_vehicule.code }}</td>
                <td>{{ erp_modele_vehicule.nom }}</td>
                <td>
                    <a href="{{ path('app_erp_modele_vehicule_show', {'id': erp_modele_vehicule.id}) }}">show</a>
                    <a href="{{ path('app_erp_modele_vehicule_edit', {'id': erp_modele_vehicule.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_erp_modele_vehicule_new') }}">Create new</a>
{% endblock %}
