<form method="post" action="{{ path('app_fournisseur_delete', {'id': agent_fournisseur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agent_fournisseur.id) }}">
    <button class="btn">Delete</button>
</form>
